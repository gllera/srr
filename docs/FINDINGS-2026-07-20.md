# SRR — Consolidated Findings (2026-07-20)

Every open finding from the four 2026-07-20 review documents, merged, deduplicated, and
re-indexed under one ID scheme. All grounded in `main` @ `615b62e`; every file:line
reference is as of that commit (a cited range may begin a line or two into a leading
comment — anchor on the range, not the exact first line). **Findings only — nothing
here has been applied.**

> **Update (2026-07-20, later):** 21 entries — SEC1, SEC2, SEC4, DEF1–DEF5, REL1, REL2,
> REL4–REL10, REL12, REL13, TST2, TST3 — were turned into the apply-plan at the repo root,
> `FINDINGS-PLAN.md`, and removed from this file. Plain-text mentions of those IDs below
> refer to that plan; everything still listed here remains an open finding.
>
> **Update (2026-07-20, later still):** 8 more entries — ARC4, FET1, FET2 (per-host
> semaphore half; the jitter half was NOT selected), FET4, GRO1, TST12, ENG1, FEB5 —
> graduated to the same plan (steps S22–S29) and were removed from this file likewise.
> FET3 remains open but is superseded in practice by the planned ARC4 (pick-one pair).
>
> **Update (2026-07-20, evening):** 6 more entries — ARC1 (**decided: (a) static store**),
> ARC2, ARC3, ARC5, ARC7, ARC8 — graduated to the regenerated `FINDINGS-PLAN.md`
> (steps S30–S40) and were removed from this file likewise. ARC6 stays open here
> (decide together with FMT6).
>
> **Update (2026-07-25):** the radical batch shipped in full — S30–S44 (generation-manifest
> cutover, physical compaction, multi-store mounting, same-origin admin console, MCP) are
> applied, released as **v4.8.0** (2026-07-22, + the v4.8.1 grace fix) and deployed; the
> prod store runs the v3 manifest root. Two entries were fixed by that work and moved to
> [Appendix A](#app-a): **SEC3** (S40's header middleware) and **PWA0** (S38's multi-root
> service worker + second-origin browser test). Remaining entries were corrected in place
> where the cutover changed their ground truth (REL3, FET3, STO1, STO6, GRO3, GRO6, FMT2,
> FMT3, FMT7, ARC6, ENG2, ENG3, Appendix E). File:line references remain as of `615b62e`
> and have drifted further with the S30–S44 diffs — anchor on symbol names, not line
> numbers (references inside the corrected entries were re-verified 2026-07-25).
>
> **Update (2026-07-25, later):** the entire **Do first** list — TST1, TST4, FMT2(a),
> FEB1–FEB4, RDR4, POL1–POL5, RDR1, RDR2, REL3, PWA1, PWA2, RDR12 (19 findings) —
> graduated to the regenerated `FINDINGS-PLAN.md` (steps S45–S62) and was removed from
> this file. Sections 3 (FEB) and 12 (POL) emptied out entirely; FMT2 remains holding
> only its unselected backend half (b).
>
> **Applied (2026-07-25):** those 18 steps are IN — `make verify`, `make test-browser`,
> `make test-race-be` and a `make fuzz-be` smoke are green — but **not released or
> deployed**. Two things learned there are worth carrying back here: the TST1 crash
> harness reproduced REL3's SIGKILL wedge on its first run (every halt point, one
> error), so that pairing was right; and REL3's conditional-write half is a REAL
> compare-and-swap only on S3/R2 (ETag + `If-Match`) — local/SFTP are documented
> best-effort, plain HTTP declines rather than pretend. See `FINDINGS-PLAN.md` →
> Applied for the rest.
>
> **Update (2026-07-26):** 12 more entries — GRO6, GRO7, STO1–STO6, RDR3, RDR7, RDR11,
> RDR14 — graduated to the regenerated `FINDINGS-PLAN.md` (steps S63–S74) and were
> removed from this file likewise. Section 7 (STO) now holds only STO7.
>
> **Update (2026-07-26, later):** section 14 (ENG) was **implemented directly** rather
> than planned — ENG2–ENG7, all six — and removed from this file; the section is gone.
> `app.ts` split into reader/pin-ui/search-ui/menus (+ the leaf `els.ts`), `nav.ts` into
> seen/saved/prefetch/route, `nav.filter`'s fields now unreachable from production code,
> a `srr-schema` boot gate, and the mutex↔fetch-timeout invariant named on both sides.
> ENG5 needed no code: it had already been done, and its open sub-question ("should
> `srr-hash` join the profile export?") was decided against and is pinned by a test.
> One bug surfaced on the way and is worth knowing about, because it silently corrupted
> the ground truth of ENG4's scope: `app.ts` contained a literal NUL byte, which made
> git render it as binary and made **every grep over it return nothing at all**.
>
> **Update (2026-07-26, later still):** section 8 (CON) went the same way — CON1 and CON2
> were **implemented directly** rather than planned, and removed from this file; the
> section is gone. `#feed` now surfaces the feed's own declared language (RSS
> `<language>`/`<dc:language>`, Atom `xml:lang`) through `ingest.Result.Lang` as the
> LOWEST-priority stamp: a per-item declared value and confident detection both still
> beat it, and the post-pipe re-detection was widened so a `#readability`-grown body can
> still upgrade a feed-level fallback. `#embed` recognizes TikTok, Twitch, SoundCloud,
> Streamable and Bandcamp on top of the original four — TikTok and Bandcamp link the
> normalized PLAYER page, the one case where the embed URL names no author/artist and so
> yields no derivable watch page.
>
> **Update (2026-07-26, final batch):** nine more entries — PWA5, RDR18, RDR10, RDR9, RDR8,
> FMT2(b), FMT3, FMT5, FMT8 — were **implemented directly** rather than planned, and removed
> from this file; sections 9 (FMT), 11 (RDR) and 13 (PWA) shrank accordingly. Three of the
> nine did not land as written, and the deviations are the part worth carrying forward:
> **FMT3 is a KEEP verdict, not a deletion** — `bg` is NOT subsumed by the seen pool, because
> a per-feed `dd = -1` disables the pool outright (leaving `bg` the only GUID memory) and
> because the pool ages out under a 304 while `bg` is re-persisted intact, so a feed quiet
> longer than the horizon loses pool coverage entirely; on a dateless feed, where `wm` is 0
> and can never floor anything, dropping `bg` would re-ingest the whole window. The
> counterfactual is pinned by tests and written up in `backend/SEEN-POOL-PLAN.md` §11.
> **PWA5 could not warm db.gz alone** — a cached root naming a generation whose tails were
> never fetched breaks the next offline launch, so the warm runs the store's own commit
> discipline client-side (cache the generation's boot set, flip the root last).
> **FMT5 landed backend-only**: rules, ingest-time evaluation, the `watch` bitmap series, GC
> and `inspect --validate`; the reader lane and the serve/MCP surfaces are still owed.
> FMT2(b)'s `ab` accounting keeps one documented residual — an asset uploaded during feed
> A's fetch but first stored in feed B's article is charged to A and released from B; closing
> it needs the uploader identity plumbed to `PutArticles`, which is not worth it for a
> display-only counter.

**Merge rules**

- 159 source findings collapsed into **123 entries**. Each entry's header line lists the
  source IDs it merges (e.g. `AR-F1 · IM-I02 · BE-F6`) for traceability back to the full write-ups.
- Where sources disagreed on priority, the higher priority won. IM's ★★★/★★/★ value
  scale maps to P1/P2/P3 (adjusted down where IM itself ranked the item lower).
- Where sources contradicted each other on substance, the correction won and is flagged
  in place. Two cases: jump-to-date ([RDR15](#rdr15) — the IM client-only proposal is
  unsound; FE's analysis stands) and the nitter request-rate figure (~4,600 doomed
  requests/day is the **total** across 16 feeds at 5-min cadence, not per feed).
- **Priority:** P0 fix/decide now · P1 do soon · P2 worthwhile · P3 opportunistic.
  **Effort:** S hours · M ~a day · L multi-day · XL needs a design doc first.

## Verdict

The core bet — write-once immutable packs + one mutable pointer object, a single writer,
dumb static reads — is **sound and proven**: CDN-cached for free, offline-capable, and
still working when every server is down. The debt is not in the bet; it is in (1) the
number of coordinated side-structures the bet has accreted (idx summaries, meta shards,
bloom summaries, delta chain, GC low-water, ping/pong seen slots, gen counter — each
with its own sync function, fallback path, and GC), and (2) a few operational seams:
purge discipline, the `.locked` SIGKILL wedge, the single writer pinning fetch egress to
one IP, and two security gaps (SEC1, SEC2 — since planned in `FINDINGS-PLAN.md`).

*(Update 2026-07-25: the manifest cutover (v4.8.0) settled much of point (1) — the `gen`
counter, the ping/pong seen slots, the purge discipline and the per-feature ordering
proofs are retired; the side-structures that remain (idx/meta summaries, delta chain, GC
low-water) now hang off one explicit name table. Of the seams: SEC1/SEC2 shipped with
S1–S29; the `.locked` SIGKILL wedge (REL3 — planned, S47–S48) and the single-writer egress pin
([FET3](#fet3) / ARC4 deployment) remain.)*

## Do first

*(2026-07-25, later: every row of this list graduated to `FINDINGS-PLAN.md` as steps
S45–S62 — TST1 + TST4 the safety net first, then REL3, the FEB/POL/RDR4 reading-surface
batch, RDR1 + RDR2 undo, FMT2(a) + PWA2 pin durability, PWA1, RDR12 — and the entries
were removed from this file. What remains below is the open backlog; see
[Appendix E](#app-e) for the sequencing of what's left.)*

---

## Index

### 4 · Reliability, durability & ops (REL)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [REL11](#rel11) | `/metrics` endpoint on `srr serve` | P2 | M |

### 5 · Fetch pipeline (FET)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [FET3](#fet3) | Per-feed egress proxy — recovers the 16 nitter feeds | P1 | M |
| [FET6](#fet6) | Network mods run serially per item | P1 | M |
| [FET7](#fet7) | No in-cycle retry for transport blips; `Retry-After` ignored | P2 | M |
| [FET8](#fet8) | Hard-coded 10 s whole-request fetch timeout | P2 | S |
| [FET9](#fet9) | Typed/classified fetch errors | P2 | M |
| [FET10](#fet10) | Batch JSONL protocol for external mods | P2 | M–L |
| [FET12](#fet12) | WebSub push (YouTube et al.) | P2 | M |
| [FET13](#fet13) | Permanent redirects never repoint the feed URL | P2 | M |
| [FET15](#fet15) | Cross-feed dedup | P3 | S |

### 6 · Growth & scaling (GRO)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [GRO2](#gro2) | Mixed retention windows re-scan the whole spread every cycle | P2 | M |
| [GRO3](#gro3) | Idx/meta summaries rebuilt O(store) on every finalization | P2 | S–M |
| [GRO4](#gro4) | No per-cycle pack read cache | P2 | S |
| [GRO5](#gro5) | Zopfli policy per series | P2 | S |

### 7 · Storage layer (STO)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [STO7](#sto7) | `HTTP.AtomicPut` atomicity is asserted, not enforced | P3 | M |

### 9 · Data contract & format evolution (FMT)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [FMT1](#fmt1) | Stop reusing feed ids | P2 | S |
| [FMT4](#fmt4) | Comments-link field (`cm`) | P2 | S |
| [FMT6](#fmt6) | Meta-card enrichment: snippet / thumbnail / lang / date checkpoints | P2 | XL |
| [FMT7](#fmt7) | Full-text content search | P2 | XL |

### 10 · Radical architecture (ARC)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [ARC6](#arc6) | Merge `idx/` and `meta/` into one derived series | P2 | L |

### 11 · Reader UX & features (RDR)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [RDR5](#rdr5) | Display settings: theme override, text size, measure | P1 | M |
| [RDR6](#rdr6) | Share / copy-link affordance | P2 | S |
| [RDR13](#rdr13) | Reading position, progress + read-time estimate | P3 | M |
| [RDR15](#rdr15) | Jump-to-date — **corrected: infeasible client-only** | P3 | blocked |
| [RDR17](#rdr17) | Web Push notifications | P3 | L |

### 13 · PWA / platform (PWA)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [PWA3](#pwa3) | Device storage usage/quota not surfaced | P3 | S |
| [PWA4](#pwa4) | iOS: landscape safe-area insets, status-bar meta | P3 | S |

### 15 · Testing, tooling & docs (TST)
| ID | Finding | Prio | Effort |
|----|---------|:--:|:--:|
| [TST9](#tst9) | Backend conformance table + minio job | P3 | M |

**Appendices:** [A — resolved / already implemented](#app-a) · [B — deliberately not proposed](#app-b) ·
[C — audited and sound](#app-c) · [D — fragile-but-correct invariants](#app-d) · [E — sequencing & synergies](#app-e)

---

## 1 · Security & privacy

<a id="sec3"></a>
*(SEC3 — admin security headers — was fixed by S40 in v4.8.0; see [Appendix A](#app-a).)*

---

## 4 · Reliability, durability & ops

<a id="rel11"></a>
### REL11 — `/metrics` endpoint on `srr serve`
`P2 · M · BE-O7 + IM-I21`
Health lives in manifest feed fields + logs; the watchdog scrapes state, and tuning work
(FET1/FET2, [GRO5](#gro5)) has nothing to measure against —
regressions like the meta-tail write amplification were found by hand in the 07-10
audit. **Fix:** Prometheus text endpoint (cycle durations, bytes fetched/written,
per-feed status transitions, GC/consolidation counters), or a per-cycle structured
report object in the store.

---

## 5 · Fetch pipeline

<a id="fet3"></a>
### FET3 — Per-feed egress proxy — recovers the 16 nitter feeds
`P1 · M · IM-I03 + BE-F12`
Deployment fact: 16 of 71 feeds (~23%, X/Twitter via nitter) fail from dmz's datacenter
IP since the fetch loop moved there on 2026-07-09 (nitter.net resets HTTP/2,
poast/xcancel WAF-403). Highest immediate user-visible payoff — the feature *worked*
until 07-09. **Fix:** a per-feed (or per-recipe) `proxy:` setting (SOCKS5/HTTP) on the
fetch transport, routing only those feeds through bastion's residential IP (WireGuard,
or an ssh `-D` tunnel dmz→bastion); the `secrets:` section in `srr.yaml` already homes
credentials. ARC4 (applied, S25 — the `inbox/` producer split, `docs/INBOX-SPEC.md`) is
the architectural alternative that fixes the same feeds without a proxy: the binary
ships it, but no bastion producer is deployed yet, so this stays open only as the
fallback if that deployment stalls.

<a id="fet6"></a>
### FET6 — Network mods run serially per item
`P1 · M · BE-F2`
`feed.go:552-587` — the second pass loops candidates one at a time: `#readability` = one
GET per item (20 s default), `#selfhost` downloads each item's media sequentially
(120 s default). Only the post-pipe *upload* is concurrent (`feed.go:644-662`) — proving
item-level fan-out is already considered safe. A 50-item teaser feed with `#readability`
can hold one worker ~17 min. **Fix:** bounded errgroup over the candidate loop with an
order-preserving indexed result slice (published-sort happens later, `cmd_fetch.go:467`).

<a id="fet7"></a>
### FET7 — No in-cycle retry for transport blips; `Retry-After` ignored
`P2 · M · AR-F4 + BE-F7`
`ingest/feed.go:63-65` — every non-200/304 is an immediate hard error; one attempt per
feed per cycle (`feed.go:256-259`), so a connection reset or a `429 Retry-After: 2`
costs a full interval and dirties `ferr`/`fail_streak`. **Fix:** one short retry for
idempotent transport-level failures (reset/EOF pre-response); honor `Retry-After` when
≤ a few seconds. Distinct from FET1 (cross-cycle cadence).

<a id="fet8"></a>
### FET8 — Hard-coded 10 s whole-request fetch timeout
`P2 · S · BE-F8`
`cmd_fetch.go:342-345` — `http.Client{Timeout: 10s}` covers dial + TLS + TTFB + entire
body read; `MaxFeedSize` is 5000 KB, so a 5 MB feed must sustain ~4 Mbit/s or fail
permanently every cycle. Not configurable; inconsistent with `#readability`'s 20 s.
**Fix:** `--fetch-timeout`/`SRR_FETCH_TIMEOUT` (default ~30 s); optionally split budgets
(`ResponseHeaderTimeout` ~10 s + generous overall).

<a id="fet9"></a>
### FET9 — Typed/classified fetch errors
`P2 · M · BE-F11`
`ferr` is a flat string (`Feed.FetchError`); all failures look alike to backoff logic,
the admin GUI, and the operator. **Fix:** classify (dns, tls, http-4xx, http-5xx,
WAF-403, parse, timeout) → smarter backoff (a WAF block is permanent-ish, a 503 isn't),
GUI grouping, alerting. Feeds FET1 and REL6 (both applied, S1–S29).

<a id="fet10"></a>
### FET10 — Batch JSONL protocol for external mods
`P2 · M–L · BE-F3`
`mod/main.go:280-352` — external mods run once per item per pipeline token: fresh
`/bin/sh -c` fork + full JSON encode of the item *including the whole parsed `Raw`
entry*, per call (external **ingest** is already batched). 100 new items × one Python
mod = 100 interpreter startups + 100 redundant serializations per step; stateful mods
(model loading, session reuse) are impossible — the real extensibility ceiling.
**Fix:** opt-in batch mode (one process per feed+step, items streamed as JSONL one line
in → one line out, order-preserving); today's per-item exec stays the compatible
default; the 64 MiB cap + `WaitDelay` hardening carries over per line.

<a id="fet12"></a>
### FET12 — WebSub push
`P2 · M · AR-F5 + IM-I14`
**Verified:** no WebSub/push anywhere; fetching is pure interval polling. dmz is already
publicly reachable through its tunnel — `srr serve` hosts a small HMAC-verified callback
route; subscribe for capable feeds (YouTube supports WebSub natively); a hub ping
triggers that one feed's fetch; the 5-min loop remains the fallback. New videos land in
seconds instead of poll-bound, and polling load drops for exactly those feeds. Pairs
with FET1.

<a id="fet13"></a>
### FET13 — Permanent redirects never repoint the feed URL
`P2 · M · AR-F3 + BE-F10`
The fetch client sets no `CheckRedirect` (`cmd_fetch.go:338-346`); net/http follows up
to 10 hops and `feedFetch` discards the final URL (`ingest/feed.go:53`). The only
repoint mechanism is HTML `<link rel=alternate>` discovery on parse failure. **Fix:**
track hops in `CheckRedirect` (pattern exists in `store/http.go`); when every hop was
301/308 for N consecutive cycles, set `Result.ResolvedURL` to the final URL — the
existing repoint path handles the rest **without** resetting the seen pool (unlike
`setFeedURL`, which deliberately clears validators). Leave 302/307 alone.

<a id="fet15"></a>
### FET15 — Cross-feed dedup
`P3 · S · IM-I17`
The seen pool dedups within a feed; the same story arriving via two feeds (or a
feed-migration overlap) isn't caught. **Fix:** an optional canonical-link-hash axis in
`seen.gz` — same columnar format, per-feed opt-in like `dt`.

---

## 6 · Growth & scaling

<a id="gro2"></a>
### GRO2 — Mixed retention windows re-scan the whole spread every cycle
`P2 · M · BE-G2`
`db_expire.go:56-80` — one global window `[minStart, maxCutoff)`: `minStart` = the
oldest `AddIdx` across expiring feeds, `maxCutoff` = the *shortest* retention's cutoff.
With `exp=365` alongside `exp=7`, ~358 days of packs are re-read every cycle (including
idle ones) to expire nothing. **Fix:** per-feed frontiers — `fetched_at` is
chron-monotone, so each feed's expirable region binary-searches (the
`cmd_art.go findWindow` precedent); walk only the union of non-empty regions — steady
state ≈ one cycle wide each.

<a id="gro3"></a>
### GRO3 — Idx/meta summaries rebuilt O(store) on every finalization
`P2 · S–M · BE-G3`
`db_pack.go:449-457` (`SyncIdxSummary` "always rebuilds from scratch by re-reading each
finalized pack's header"); `SyncMeta` likewise "rebuilds the bloom summary from cheap
streaming header reads" whenever a shard finalizes (`db_meta.go:196-199`) — at 1M
articles each 5k-article meta finalization issues ~200 GETs + a ~800 KB summary
re-upload while the just-computed bloom is discarded from memory. O(store) work per
fixed-size increment = quadratic cumulative cost; unchanged by the cutover (summaries
now land under fresh stems, but each is still built from scratch). **Fix:** extend
incrementally — GET the previous summary, append the in-memory bloom/header (gzip
members concatenate legally), publish under the fresh stem; keep from-scratch as the
missing/self-heal fallback (already the story). Same shape for the idx header summary
(`names.hsum`).

<a id="gro4"></a>
### GRO4 — No per-cycle pack read cache
`P2 · S · BE-G4`
`walkArticles` (`db_meta.go:141-158`) does `readGz` + parse per call, no memo; called by
`ExpireArticles`, once per managed out-feed, and by `SyncMeta` catch-up — only the delta
chain is memoized (`deltaMemo`, `db.go:207-216`). **Fix:** cycle-scoped `map[key]→parsed pack` on
`DB` (reset where `deltaMemo` resets); finalized packs are immutable so even a
cross-cycle LRU is safe. Multiplies GRO1/[GRO2](#gro2)'s wins.

<a id="gro5"></a>
### GRO5 — Zopfli policy per series
`P2 · S · BE-G5`
`db_pack.go:330-358` — go-zopfli (pure Go) runs on every finalized data pack, every 50k
idx pack, and **every 5k meta shard**, inline under the lock, on ARM hosts; `gzipBest`
also gunzips input + round-trips output (two extra inflations per finalization).
**The measurement now exists** (`make bench-be`, `BenchmarkGzipBest`) and it is damning
— x86 i9-14900K, savings stated against the stdlib-default bytes `savePack` already
writes; the ARM SBC that actually pays this is several times worse:

| series | zopfli | saved | stdlib `BestCompression` | saved |
|---|---|--:|---|--:|
| idx | 730 ms | 2.4% | 4.4 ms | 0.0% |
| data | 3.38 s | 6.7% | 57 ms | 0.1% |
| meta | 1.52 s | 9.6% | 40 ms | 0.5% |

36–170× the CPU for a few percent, ~500 MB allocated per data pack. ⚠ **The idx row is
a LOWER BOUND, not a verdict**: an idx pack is only ordered feed ids, so its
compressibility is entirely an ordering property, and the benchmark's fixture draws from
`genbig_test.go`'s long-tailed distribution rather than the publication-time-sorted runs
a real batch writes — which is why it reads 2.4% where the original audit measured
−11%. Confirm against a real pack from `cdn.llera.eu` before deciding. The data/meta
rows carry real article bytes and need no such asterisk. **Fix:** per-series policy
(`finalGzip` is already the seam) — plausibly keep zopfli for idx alone, stdlib for
data/meta. Names are write-once + cached forever, so the policy knob is the whole fix.

---

## 7 · Storage layer

<a id="sto7"></a>
### STO7 — `HTTP.AtomicPut` atomicity is asserted, not enforced
`P3 · M · BE-S7`
`http.go:146-150` — "the server makes the object visible atomically on completion" is
assumed, never arranged. True for S3-style PUT; a WebDAV/file-mapped server may write in
place, letting a reader observe a truncated db.gz — the exact failure `Local.AtomicPut`
spends fsync+rename to prevent, on the one mutable object every reader polls. **Fix:**
opt-in mode: PUT to `uniqueTempName(key)` + WebDAV `MOVE` (+`Overwrite: T`), falling
back to plain PUT; reuse the temp-name grammar so sweep logic (and STO1's `List` —
planned, S64) covers leftovers. At minimum, document the assumption.

---

## 9 · Data contract & format evolution

<a id="fmt1"></a>
### FMT1 — Stop reusing feed ids
`P2 · S · AR-D1`
Feed-id reuse after removal is a documented hazard class: the
`RemoveFeed → DrainDeltas` header-corruption guard, "entries below `add_idx` arise from
id reuse". Monotonic-forever allocation deletes the whole class. At ~71 feeds against a
65,536 ceiling, reuse buys nothing; dead slots cost 4 bytes per pack header each.

<a id="fmt4"></a>
### FMT4 — Comments-link field
`P2 · S · IM-I15`
RSS `<comments>` (Hacker News, Lobsters, Reddit) is currently discarded. Add a `cm`
short key to `ArticleData` and a "comments" affordance next to the reader permalink.
Small contract change; makes discussion-centric feeds first-class. (An `author` field is
the same shape if ever needed; comments is the higher-value half.)

<a id="fmt6"></a>
### FMT6 — Meta-card enrichment: snippet / thumbnail / lang / date checkpoints
`P2 · XL · FE-R3 + IM-I05 + parts of IM-I04, IM-I08`
List rows show source · age · title because meta cards carry nothing else. Related wants
that all land in the same struct — decide the fields **once** (repeated meta format
bumps are the expensive part): a ~140-char folded **snippet** for a scannable dek; an
optional **lead-image asset key** for list thumbnails rendered through the deployed
imagor at `img.32b.io` (on-the-fly resize, URL-rewrite rule already in place — a
scannability win for YouTube/Telegram-heavy stores, at the cost of per-card bytes and a
design question vs the wire-service aesthetic); per-article **`g`** for language lanes;
per-shard **date→chron checkpoints** to unblock jump-to-date ([RDR15](#rdr15)).
Trade-off: meta shards grow ≈200 KB → 500 KB+ gz; `META_KEEP` (80) in the SW is the
tight bucket. **Proposal:** design doc weighing snippet-only vs snippet+thumb; prototype
snippet-only first. Note [ARC6](#arc6) touches the same series — decide together.

<a id="fmt7"></a>
### FMT7 — Full-text content search
`P2 · XL · IM-I01 + FE-R2 + AR-T2`
Search is titles-only by construction: meta cards are `{f,w,t}` and the blooms fold
title trigrams (`search.ts:1`); article body `c` is never indexed. In a store whose
retention is "forever", not finding a remembered phrase is the largest genuine
capability gap — radical in value, conservative in architecture, because the template
already exists (derived write-once bloom-pruned shards, warn-only sync, manifest-listed
fresh stems, the one-rule GC, SW grammar). Two shapes to evaluate: (a) widen the per-shard bloom to also
fold content trigrams (likely 16–32 KB/shard vs today's 4 KB; confirmation reads against
`data/` packs for candidate shards only), or (b) a parallel `fts/` series
(pagefind-style chunked inverted index built at consolidation also fits). Design doc first.

---

## 10 · Radical architecture

<a id="arc6"></a>
### ARC6 — Merge `idx/` and `meta/` into one derived series
`P2 · L · AR-S2`
`idx/` and `meta/` are both chron-ordered projections of `data/` at different strides
(50k vs 5k), each with its own header scheme, summary singleton (`names.hsum` /
`names.ssum`), sync function, manifest state (`hsum.covers` vs `mt` + `ssum.covers`),
and reader fallback path (the per-series GC sweeps did merge into the one-rule GC at
the cutover). One card series at 5k stride — per-shard header carrying cumulative
per-feed counts, data-pack boundary info, and the bloom, then the `{f,w,t}` cards —
replaces all of it. The cutover deliberately kept this door open: series live in a map
in the manifest and nothing assumes there are three of them (`MANIFEST-SPEC.md` calls
it the ARC6 door) — the merge is now a manifest-shape change and nothing else.
**Trade-off:** idx's 2-byte entries are bandwidth-optimal for navigation and cards are
fatter to scan within a shard — but cumulative counts at 5k grain (10× finer than
today) let filtered navigation locate the target shard **from the summary alone**, and
the shard it then fetches is one the list view needs anyway. Biggest pure
contract-shrink available; thins exactly the code that needs dedicated review agents
today, and shrinks the surface the generated idx parsers (FMT8, shipped) and TST4's fuzzers
(planned, S46) must cover. Decide together with [FMT6](#fmt6) (same series).

---

## 11 · Reader UX & features

<a id="rdr5"></a>
### RDR5 — Display settings: theme override, text size, measure
`P1 · M · IM-I07 + FE-F2`
Dark mode is `prefers-color-scheme`-only with no override (3 dark-scoped blocks,
`tokens.css:99,110` + `styles.css:2074`); no font-size control (fixed scale
`tokens.css:31-34`), fixed 680px
measure (`styles.css:76,1014`). The design harness already implements an Auto/Light/Dark
switch, so the token plumbing is proven. **Fix:** a small "Display" card off the
settings menu (theme override, text size, optionally measure), persisted in localStorage
(`keys.ts`) and carried in the profile blob as an opt-in pref (the `srr-img-proxy`
pattern; blob `v:2`→`v:3`). Small work, touched every day.

<a id="rdr6"></a>
### RDR6 — Share / copy-link affordance
`P2 · S · IM-I10 + FE-F3`
No `navigator.share`, no copy-URL affordance anywhere (grep empty); the masthead
permalink + `f` key is the only exit. **Fix:** one frontier-menu or toolbar entry —
`navigator.share({url: article.link})` where available, clipboard fallback.

<a id="rdr13"></a>
### RDR13 — Reading position, progress + read-time estimate
`P3 · M · IM-I12 + FE-F10 + FE-D7`
Every reader render hard-resets to top (`app.ts:346-349,409` — deliberate landing
model); no per-article scroll memory, no reading-progress indicator, no visited-history
surface. **Fix:** session-level scroll restore when *returning* to a half-read article
(keep top-landing for fresh opens); a thin scroll-progress bar; a word-count
reading-time estimate in the dateline (cheap client-side at render); an in-session
visited ring could power a small "recently read" lane with no new persistence.

<a id="rdr15"></a>
### RDR15 — Jump-to-date — **corrected: infeasible client-only**
`P3 · blocked · IM-I08 + FE-F12`
Deep history is practically unreachable — the only way to last May is scrolling. IM
proposed a client-only binary search over meta `w` timestamps; **FE's analysis overturns
it**: chron is *fetch* order and `w` is *publish* time — not co-monotonic (a late-added
feed backfills old-dated articles at high chrons), so a binary search over shard
first/last `w` is unsound. **Do not build the client-only version.** Only viable with a
backend-emitted date→chron checkpoint sidecar — folded into the [FMT6](#fmt6) format
decision (tappable sticky day dividers become the entry point once checkpoints exist).

<a id="rdr17"></a>
### RDR17 — Web Push notifications
`P3 · L · IM-I06`
Per-tag/feed push subscriptions so new articles notify without the app open. Needs a
live origin — a break from pure-static — but `srr serve` already runs 24/7 on dmz
behind a public tunnel, and the reader already ships a service worker: VAPID
subscription management on serve; the fetch loop fires pushes on new matches. Ranked
low because the 5-minute refresh loop plus PWA already covers most of the need at zero
architectural cost; RDR12 (planned, S62) is the cheap 80%.

---

## 13 · PWA / platform

<a id="pwa0"></a>
*(PWA0 — the service worker inert in production behind its same-origin early return —
was fixed by S38's multi-root worker in v4.8.0, with a second-origin browser test; see
[Appendix A](#app-a). Its downstream consequence, PWA2 — pins evictable because
`storage.persist()` is never requested — became real again with that fix and is planned,
S60; PWA1's update toast is planned, S61.)*

<a id="pwa3"></a>
### PWA3 — Device storage usage/quota not surfaced
`P3 · S · FE-P3`
Info dialogs show source-side `cb`/`ab` byte counters but never
`StorageManager.estimate()`. Show `estimate().usage/quota` in the pin row / store info card.

<a id="pwa4"></a>
### PWA4 — iOS: landscape safe-area insets, status-bar meta
`P3 · S · FE-P5`
`safe-area-inset-top/bottom` handled (`styles.css:11,437,2006`); `left/right` absent
(landscape notch can clip); no `apple-mobile-web-app-status-bar-style` meta. Add
horizontal insets to container padding + the status-bar meta.

## 15 · Testing, tooling & docs

<a id="tst9"></a>
### TST9 — Backend conformance table + minio job
`P3 · M · BE-V8`
`store/s3_test.go:24,138` — in-memory S3 fake only; exclusive-create is re-implemented
bespoke 4× across the backend tests — parity by diligence, not construction. Prod runs
on R2, whose semantics already bit once (jurisdiction endpoint). **Fix:** shared
`testBackendConformance(t, be)` table over all four backends; opt-in CI job against a
minio service container (`SRR_S3_TEST_ENDPOINT`, skipped when unset).

---

<a id="app-a"></a>
## Appendix A — Resolved / already implemented (do not re-propose)

Verified during the 2026-07-20 reviews:

**Fixed since earlier audits**
- **SEC3 — admin origin ships no security headers** (this file, graduated 2026-07-25):
  fixed in v4.8.0 (S40) — `srr serve` wraps every response, 403s included, in
  CSP + nosniff + Referrer-Policy middleware (`cmd_serve.go` `secHeaders`/`webUICSP`),
  and the admin console became a same-origin embedded Parcel bundle.
- **PWA0 — service worker inert in production** (this file, graduated 2026-07-25): fixed
  in v4.8.0 (S38) — the multi-root worker routes every mounted store's pack origin,
  cross-origin included, through cache-first (`sw.ts` mount table + the page's
  `{type:"mounts"}` post), and the browser e2e layer now serves packs from a second
  origin so the regression class stays visible.
- **Retired wholesale by the manifest cutover** (v4.8.0 — do not re-propose fixes to
  them): the `gen` counter and `srr gen --bump`, the CDN purge discipline, the ping/pong
  seen slots, the `db/` snapshot series, and the four GC window formulas.
- **Meta-tail ~100× write amplification** (2026-07-10 audit): gone — delta cycles skip
  `SyncMeta` entirely (`db_meta.go:253-267`); the meta tail is rewritten only at consolidation.
- **Uncapped image prefetch**: fixed — 6 images / 2 videos per neighbor, idle-scheduled,
  aborted on navigation (`nav.ts:728-792`).
- **No feed backoff**: partially fixed — dormancy backoff exists for quiet feeds
  (`cmd_fetch.go:185-213`); the remaining gap is failure backoff (FET1).

**Already present (checked before proposing)**
- Cross-device sync of read/saved state (`sync.ts` over a user-supplied endpoint)
- Offline pinning per filter (`pin.ts` + SW pinned bucket)
- Title search with bloom-pruned shards (`search.ts`)
- Concurrent fetch (errgroup fan-out); conditional GET (etag/last-modified)
- OPML import/export (`feed import` / `feed export`)
- Admin GUI: feed CRUD, overview, fetch-now, preview, import/export, dedup, syndication
- Feed autodiscovery (`ingest/feed_discover.go`)
- Saved-articles queue with save-order traversal and ghost anchors
- Live refresh (focus / online / 5-minute heartbeat, `refresh.ts`)
- Image-proxy option; backup/restore profile dialog
- Syndication outputs (RSS 2.0 / JSON Feed, plus external push slots)
- Language detection backend-side (`g` in data packs; reader consumption is RDR4 — planned, S52)

<a id="app-b"></a>
## Appendix B — Deliberately NOT proposed / parked

**Architecture & product decisions to leave alone**
- Pluggable store backends (local/S3/SFTP/HTTP)
- The zero-dep frontend constraint (the code quality justifies it — no framework)
- The recipe/pipe shell-command model; the monolith CLI
- The delta-tail design itself (it is the right WAL — ARC4 builds *on* it)
- The finalized-zopfli vs fast-tail compression split (per-series compressor choice is
  [GRO5](#gro5)'s separate, compatible question)
- The three-layer e2e split (contract / browser / stress)
- The read-state sync *model* (localStorage + LWW merge — RDR18 shipped its two nits:
  a first-party endpoint on `srr serve` and per-key saved-set stamps)
- The frontier seen-model (per-article sets would kill its size/syncability — RDR1's
  undo, planned S57, adds reversibility instead)
- Merging list+reader into continuous scroll (the two-surface model is the most-iterated decision)
- Feed CRUD in the reader (deliberate boundary; ARC8 landed instead as the same-origin
  admin console embedded in `srr serve` — S39–S40, v4.8.0)
- Network-request reduction (previously audited near-minimal; remaining ideas confirmed illusory)
- Webfonts / font subsetting (system stacks documented deliberate, `tokens.css:24-26`)
- Resource-locking CSP (incompatible with user-configured proxy/sync/remote media —
  SEC2 (applied) covers the viable subset)

**Parked ideas (revisit on demand)**
| Idea | Why parked |
|------|-----------|
| Cross-feed story clustering | Folded-title machinery exists (seen pool `dt`), but UX payoff unclear at ~70 feeds |
| Highlights / annotations | Profile-sync could carry them as a v3 blob, but a whole new interaction surface for modest value |
| AI per-article scoring / triage | The daily digest already answers "what mattered today"; if revisited, write scores into the watch bitmap sidecar FMT5 shipped (`backend/watch.go`) |
| Text-to-speech listen mode | Niche; `speechSynthesis` quality varies widely |
| Brotli/zstd packs | Browser `DecompressionStream` is gzip/deflate-only baseline; zopfli already squeezes finals |
| Mark-read-on-scroll | Divisive; only worth it off-by-default, low demand |

<a id="app-c"></a>
## Appendix C — Audited and sound (leave alone)

Verified under adversarial reading; no finding warranted:

**Backend**
- **SSRF architecture** — dial-time `Control` hook (no rebinding TOCTOU), redirect
  re-check, feed client guarded too; only the range table is short (SEC4, applied)
- **Marker containment** — single place, symlink/`..` guards (`assets.go:183-207`)
- **Subprocess hardening** — output caps, `WaitDelay`, stderr tail; only the
  process-group kill is missing (DEF4, applied)
- **seen.gz eviction** — single O(pool) pass, day-granularity dirty tracking, per-feed
  cap sacrifices only the offender
- **db.gz marshal cost** — tens of KB at real feed counts; not a hot spot
- **Consolidation replay crash-safety** — mid-replay failure aborts before Commit;
  deterministic re-replay overwrites identical bytes
- **Delta-tail write amplification** — bounded per the spec's honest ledger
  (`docs/DELTA-TAIL-SPEC.md` §15), unlike GRO1–[GRO3](#gro3)
- **`#filter`** — all params parsed before any condition evaluates
- **Cross-language fold/bloom parity** — pinned byte-for-byte on both sides
- **`hostGuard`** — loopback-Host-always + unforgeable `Sec-Fetch-Site` carve-out is
  sound for the tunnel topology
- **External-ingest wire protocol** — output cap / empty-stdout / nil-item handling
  defensively complete
- **Compression bombs; fetch charset** — handled (header-charset-without-XML-decl is the
  one rare gap)

**Frontend**
- **`tallyWith` counting discipline** — one body behind picker badges and the pending
  pill, brute-force differential oracle + >50k parity suite; intricate but correct and fenced
- **`guard()`/`guardBg()` mutex + 30 s fetch aborts** — considered fix for the "swipe
  stops working" bug; invariants load-bearing
- **CSS hygiene** — token-driven, ~zero dead selectors, 4 `!important`s; the 2127-line
  size is not a problem worth solving
- **Degradation paths** — offline-silent, status-footer background, retryable popup
  foreground; consistent and deliberate
- **Module graph** — acyclic, documented, side-effect-free hubs protecting unit-testability
- **Render path** — rows via `createElement`, incremental, bounded batches; only row
  *retention* is the issue (ENG1)
- **Link security** — `noopener noreferrer` on every content anchor; masthead href
  re-validated against `URL_DENY`
- **Boot waterfall** — db.gz preload + parallel delta/idx/summary fetches; no wins left

<a id="app-d"></a>
## Appendix D — Fragile-but-correct invariants (respect during any refactor)

- **`buildLatestIdx` byte splice** (`data.ts:354-385`) — synthesizes one logical idx
  pack from tail bytes + delta entries; layout-coupled to the format constants. Any
  format change must revisit it.
- **`deltaLoad` promise vs `deltaArts` array ordering** (`data.ts:535-552,623-647`) —
  delta-region reads must go through the *promise*; "simplifying" to the array
  reintroduces a throw-window during refresh.
- **`refresh()` rollback snapshot** (`data.ts:257-288`) — a 10-field list that must stay
  exhaustive; new derived state added to `applyDb` must join it.
- **`na` cross-check** (`data.ts:221-223`) — fails loudly on delta/db drift by design;
  don't soften it.
- **60 s mutex / 30 s fetch-timeout margin** — document, don't narrow. `app.ts`'s
  `BUSY_STUCK_MS` and `data.ts`'s `FETCH_TIMEOUT_MS` now name each other and state the
  direction of the coupling (raising the fetch budget requires raising the mutex
  threshold, never the reverse); deriving one from the other is deliberately NOT done —
  `app.test.ts` replaces `./data` with a mock factory, so a derived value reads
  `undefined` under test and silently disables the mutex.

<a id="app-e"></a>
## Appendix E — Sequencing & synergies

1. **Decisions:** ARC1 the static-vs-server fork — **decided 2026-07-20: (a), static
   store** (recorded via plan step S30). (The SEC1 privacy stance is decided too — see
   `docs/STORE-VISIBILITY.md`.)
2. **Prod pain — the nitter/WAF cluster from four angles:** ARC4 + FET4 + FET2 + FET1
   are all **applied** (S22–S25, shipped); [FET3](#fet3) stays open only until a bastion
   inbox producer is actually deployed.
3. **Growth curve, before the store doubles:** GRO1 (applied, S26) → [GRO4](#gro4) →
   [GRO3](#gro3) → [GRO5](#gro5) — whose measurement is no longer a prerequisite:
   `make bench-be` is in, and its numbers are quoted in the entry.
4. **Reading-surface batch:** FEB1–FEB4 + POL1–POL5 + RDR4 — **planned** (S49–S56, one PR).
5. **Read-state trust:** RDR1 + RDR2 — **planned** (S57–S58); RDR3 (the "N new"
   pill — planned, S71) rides naturally after.
6. **PWA earn-out:** PWA1, PWA2, RDR12 — **planned** (S60–S62), with FMT2(a)'s save-time
   pinning (S59) in the same pin-path cluster.
7. **Frontend tooling:** done — TST12 (applied, S28), then TST13 + TST14 applied
   2026-07-26 alongside the rest of the testing/tooling section.
8. **Structural pre-work, then radical:** OVERTAKEN — the radical chain shipped first
    (S31–S35 manifest + compaction, S36–S38 multi-store, S39–S44 console + MCP, all in
    v4.8.0) *without* REL3 or STO1; REL3 (S47–S48) landed after, and STO1 (planned,
    S64) remains a hardening follow-up rather than a prerequisite.
    [ARC6](#arc6) is the biggest long-term simplification left (the manifest kept its
    door open by design); [FMT6](#fmt6)/[FMT7](#fmt7) via one combined design doc.
9. **Continuous:** TST1 (crash harness) and TST4 (fuzzers) — **planned** (S45–S46),
    sequenced first in the plan as the safety net for its own write-path steps.
10. **Everything else opportunistically** — the S-effort items ([FMT1](#fmt1),
    [FET8](#fet8), [GRO5](#gro5), …)
    fit any idle slot.

**Synergies**
- ARC4 subsumes the urgency of FET1 for the feeds that matter;
  [FET3](#fet3) is its cheap targeted alternative — pick one.
- [ARC6](#arc6) shrinks the surface the generated idx parsers (FMT8, shipped) and TST4's
  fuzzers (planned, S46) must cover, and shares a format decision with [FMT6](#fmt6).
- [GRO4](#gro4) multiplies GRO1/[GRO2](#gro2); [GRO5](#gro5)'s gating benchmark is in.
- RDR1's undo snackbar covers RDR2 for free (both planned, S57–S58); RDR12 (planned,
  S62) is the cheap 80% of [RDR17](#rdr17).
