{
  "name": "frontend",
  "version": "1.0.0",
  "source": "src/index.html",
  "targets": {
    "default": {
      "distDir": "../dist/srrf",
      "publicUrl": "."
    }
  },
  "scripts": {
    "dev": "concurrently -k npm:serve npm:servep",
    "serve": "SRR_CDN_URL=${SRR_CDN_URL:-http://localhost:3000} parcel serve src/index.html src/design.html --no-cache",
    "servep": "npx serve --cors -l 3000 \"$(node parcel/resolve-store.js)\"",
    "build": "parcel build --no-cache --no-source-maps",
    "test": "vitest run",
    "test-contract": "vitest run --config vitest.contract.config.ts",
    "test-browser": "vitest run --config vitest.browser.config.ts",
    "test-stress": "vitest run --config vitest.stress.config.ts",
    "gen-design": "vitest run --config vitest.design.config.ts",
    "shoot-design": "vitest run --config vitest.design-shots.config.ts",
    "lint": "eslint src/js",
    "format": "prettier --write src",
    "format-check": "prettier --check src"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@parcel/packager-raw-url": "^2.16.4",
    "@parcel/transformer-webmanifest": "^2.16.4",
    "concurrently": "^9.2.1",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^29.0.1",
    "parcel": "^2.16.4",
    "prettier": "^3.8.1",
    "puppeteer": "25.0.2",
    "serve": "^14.2.6",
    "typescript-eslint": "^8.58.0",
    "vitest": "^4.1.2"
  }
}
